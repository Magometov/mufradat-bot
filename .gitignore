.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.env
node_modules/
dist/
media/
# Дампы базы: make backup кладёт их в корень.
*.sql
# Признак техработ — состояние сервера, а не код.
flags/maintenance
# Только STATIC_ROOT: собранное collectstatic. Правило без пути прятало бы и static/
# самих приложений — их стили тогда не доехали бы до сервера.
/backend/static/
.DS_Store
